import 'package:flutter/material.dart';

class HeaderSeperator extends StatelessWidget {
  const HeaderSeperator({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
