import 'package:flutter/material.dart';

class WidthSeperator extends StatelessWidget {
  const WidthSeperator({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 10,
    );
  }
}
